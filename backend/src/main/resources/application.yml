logging:
  level:
    org.springframework.web.filter.CommonsRequestLoggingFilter: "debug"
spring:
  application:
    name: moneytracker

  devtools:
    restart:
      poll-interval: 2s # hogy biztos ne induljon el 2x a rebuild
      quiet-period: 1500ms

  datasource:
    url: jdbc:mysql://db:3306/moneytracker
    username: root  # docker-compose.dev.yml-ben nincs megadva username, de alapérték szerint root
    password: root  # TODO legyen kiszervezve környezeti változóba

  jpa:
    hibernate:
      #ddl-auto: validate  # validate: ellenőrzi a séma megfelelőségét, de nem módosítja azt
      ddl-auto: update  # validate: ellenőrzi a séma megfelelőségét, de nem módosítja azt
    show-sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration  # Flyway migrációk helye
